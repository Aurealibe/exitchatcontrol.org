# Plan — quick wins + e2e profond (2026-07-10)

Issu de l'audit socratique post-rebuild (chaque item = un défaut prouvé, pas supposé).

## Quick wins (exécutés dans ce plan)

- [x] **QW1 · og.png + apple-touch-icon.png** — générés depuis le design system même
  (rig `scripts/og.html` + Chrome headless 1200×630 / 180×180), référencés dans `<head>`.
- [x] **QW2 · Artefact offline réellement fonctionnel** — bug : les `import` étaient
  commentés sans inliner → JS mort. Fix : retirer les modules React du fichier offline
  et injecter un script vanilla (~40 lignes) qui redonne TOUTE l'interactivité
  (langue, thème, filtre, checklist persistée, détails à l'impression) ; le lien
  « télécharger » y pointe vers l'origine canonique.
- [x] **QW3 · Impression complète** — `beforeprint`/`afterprint` ouvre/referme tous les
  `<details>` (le CSS seul ne peut pas imprimer le contenu d'un details fermé).
- [x] **QW4 · Titre par langue** — `setLang` met à jour `document.title` (FR/EN).
- [x] **QW5 · `<noscript>`** — bannière bilingue honnête (contenu FR affiché par défaut,
  les deux langues sont dans le DOM, lien artefact offline).
- [x] **QW6 · TOC** — « (1999-2026) » → « (1993-2026) » (aligné sur la timeline).
- [x] **QW7 · `.well-known/security.txt`** + **CI GitHub Actions** (lint + test + build).

## Batterie e2e (nouvelle, scriptée, rejouable)

- [x] **`scripts/e2e.mjs`** — build → `vite preview` → deux volets :
  - **Statique (sans JS)** : les 26 ancres de section présentes dans le HTML prérendu ;
    les DEUX langues dans le DOM ; zéro référence CDN/externe dans les assets ;
    sitemap/robots/canonical/hreflang présents ; artefact offline sans `import` cassé.
  - **Dynamique (hydraté)** : hook `?e2e=1` (module dédié, chargé seulement avec le
    param) qui clique réellement les handlers React post-hydratation — bascule EN,
    filtre 🔴 (cache une carte 🟢, garde Tor), coche une case (relit localStorage),
    bascule thème — et écrit `E2E:PASS/FAIL` dans `document.title`, lu via `--dump-dom`.
- [x] **`scripts/check-links.mjs`** — HEAD/GET sur les 23 sources primaires de la
  timeline + sources du footer ; 404/5xx/DNS = échec, 403/429 (anti-bot) tolérés.

## Gate final

- [x] lint 0 warning · vitest vert · build + prerender · e2e PASS · liens vérifiés ·
  captures visuelles fraîches · commit(s) conventionnels.
